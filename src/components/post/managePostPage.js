const React = require('react');

const ManagePostPage = React.createClass({
    render: function () {
        return (
            <h1>Manage Post</h1>
        );
    }
});

module.exports = ManagePostPage;