const React = require('react');

const HomePage = React.createClass({
    render: () => {
        return (
            <div>
                <h1>Home page</h1>
            </div>
        );
    }
})

module.exports = HomePage;