const React = require('react');

const AboutPage = React.createClass({
    render: () => {
        return (
            <div>
                <h1>about page</h1>
            </div>
        );
    }
});

module.exports = AboutPage;